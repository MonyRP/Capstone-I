var canvas;
var winWidth = 0.0;
var winHeight = 0.0;
var angle;
var pos;
var vel;
var acc;
var t;
var h;
var d;
var ball;


function setup() {
  canvas = createCanvas(windowWidth, windowHeight);
  background(50, 50, 50);
  rectMode(CENTER);
  pos = createVector(15, 0, [z])
}


function draw() {
  background(50, 50, 50);


}
